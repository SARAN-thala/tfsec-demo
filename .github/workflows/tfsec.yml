name: 'TFsec'

on:
  push:
    branches:
      - "main"
  pull_request:

jobs:
  tfsec:
    name: 'TFsec'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: aquasecurity/tfsec-action@v1.0.2
